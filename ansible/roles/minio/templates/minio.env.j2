# MinIO environment (systemd EnvironmentFile)
MINIO_ROOT_USER={{ minio_root_user }}
MINIO_ROOT_PASSWORD={{ minio_root_password }}
MINIO_VOLUMES={{ minio_data_mount }}

# Αν θα βάλουμε reverse proxy/https αργότερα, μπορούμε να ορίσουμε:
# MINIO_SERVER_URL=https://cinemonkey.com
